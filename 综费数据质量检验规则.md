# 综费数据质量检验规则

---

## 一、源头数据完整性检验

### 1.1 OA报销数据（tztek_t_personcost_bx）

| 规则编号 | 检验项 | 检验SQL | 期望结果 | 严重级别 |
|---------|--------|---------|---------|---------|
| BX-001 | 审核日期为空 | `SELECT COUNT(*) FROM tztek_t_personcost_bx WHERE LEFT(TJRQ,4)='2026' AND FIDATE IS NULL` | 0 | 严重 |
| BX-002 | 科目为空 | `SELECT COUNT(*) FROM tztek_t_personcost_bx WHERE LEFT(TJRQ,4)='2026' AND FINTYPE IS NULL` | 0 | 严重 |
| BX-003 | 事业部为空 | `SELECT COUNT(*) FROM tztek_t_personcost_bx WHERE LEFT(TJRQ,4)='2026' AND SYBMC IS NULL` | 0 | 严重 |
| BX-004 | 金额为空或非数字 | `SELECT COUNT(*) FROM tztek_t_personcost_bx WHERE LEFT(TJRQ,4)='2026' AND (ACTUALAMOUNT IS NULL OR ISNUMERIC(ACTUALAMOUNT)=0)` | 0 | 严重 |
| BX-005 | 金额为负数 | `SELECT COUNT(*) FROM tztek_t_personcost_bx WHERE LEFT(TJRQ,4)='2026' AND CAST(ACTUALAMOUNT AS FLOAT) < 0` | 0 | 警告 |
| BX-006 | 提交日期格式异常 | `SELECT COUNT(*) FROM tztek_t_personcost_bx WHERE LEFT(TJRQ,4)='2026' AND ISDATE(TJRQ)=0` | 0 | 严重 |
| BX-007 | 入职代码为空 | `SELECT COUNT(*) FROM tztek_t_personcost_bx WHERE LEFT(TJRQ,4)='2026' AND WORKCODE IS NULL OR WORKCODE=''` | 0 | 警告 |

### 1.2 OA付款数据（TZTEK_T_OADT_PayWkFlow）

| 规则编号 | 检验项 | 检验SQL | 期望结果 | 严重级别 |
|---------|--------|---------|---------|---------|
| FK-001 | 科目为空 | `SELECT COUNT(*) FROM TZTEK_T_OADT_PayWkFlow WHERE LEFT(TJRQ,4)='2026' AND FYLB IS NULL` | 0 | 严重 |
| FK-002 | 事业部为空 | `SELECT COUNT(*) FROM TZTEK_T_OADT_PayWkFlow WHERE LEFT(TJRQ,4)='2026' AND SYBMC IS NULL` | 0 | 严重 |
| FK-003 | 付款金额为空或非数字 | `SELECT COUNT(*) FROM TZTEK_T_OADT_PayWkFlow WHERE LEFT(TJRQ,4)='2026' AND (FKJE IS NULL OR ISNUMERIC(FKJE)=0)` | 0 | 严重 |
| FK-004 | 费用大类为空 | `SELECT COUNT(*) FROM TZTEK_T_OADT_PayWkFlow WHERE LEFT(TJRQ,4)='2026' AND FYDL IS NULL` | 0 | 严重 |
| FK-005 | 流程编号重复 | `SELECT LCBH, COUNT(*) FROM TZTEK_T_OADT_PayWkFlow WHERE LEFT(TJRQ,4)='2026' GROUP BY LCBH HAVING COUNT(*)>1` | 0行 | 警告 |
| FK-006 | 提交日期格式异常 | `SELECT COUNT(*) FROM TZTEK_T_OADT_PayWkFlow WHERE LEFT(TJRQ,4)='2026' AND ISDATE(TJRQ)=0` | 0 | 严重 |

### 1.3 阿里商旅数据

| 规则编号 | 检验项 | 检验SQL | 期望结果 | 严重级别 |
|---------|--------|---------|---------|---------|
| ALI-001 | 机票结算金额为空 | `SELECT COUNT(*) FROM TZTEK_T_OADT_AliAPTkt WHERE LEFT(结算时间,4)='2026' AND 结算金额 IS NULL` | 0 | 严重 |
| ALI-002 | 机票出行人入职代码为空 | `SELECT COUNT(*) FROM TZTEK_T_OADT_AliAPTkt WHERE LEFT(结算时间,4)='2026' AND 出行人入职代码 IS NULL AND 预订人入职代码 IS NULL` | 0 | 严重 |
| ALI-003 | 住宿企业结算金额为空 | `SELECT COUNT(*) FROM TZTEK_T_OADT_AliHotel WHERE LEFT(结算时间,4)='2026' AND 企业结算金额 IS NULL` | 0 | 严重 |
| ALI-004 | 打车结算金额为空 | `SELECT COUNT(*) FROM TZTEK_T_OADT_AliRtCar WHERE LEFT(结算时间,4)='2026' AND 结算金额 IS NULL` | 0 | 严重 |
| ALI-005 | 国际出差结算金额为空 | `SELECT COUNT(*) FROM TZTEK_OA_InternationBusinessTrips WHERE LEFT(申请时间,4)='2026' AND 结算金额 IS NULL` | 0 | 严重 |

### 1.4 EBS应付数据

| 规则编号 | 检验项 | 检验SQL | 期望结果 | 严重级别 |
|---------|--------|---------|---------|---------|
| EBS-001 | 付款金额为空或非数字 | `SELECT COUNT(*) FROM TZTEK_T_EBS_PO_AP_list WHERE LEFT(申请日期,4)='2026' AND (付款金额_本币含税 IS NULL OR ISNUMERIC(付款金额_本币含税)=0)` | 0 | 严重 |
| EBS-002 | 项目号为空 | `SELECT COUNT(*) FROM TZTEK_T_EBS_PO_AP_list WHERE LEFT(申请日期,4)='2026' AND 项目号 IS NULL` | 0 | 警告 |
| EBS-003 | 采购订单号为空 | `SELECT COUNT(*) FROM TZTEK_T_EBS_PO_AP_list WHERE LEFT(申请日期,4)='2026' AND 采购订单号 IS NULL` | 0 | 警告 |

---

## 二、费用明细表（TZTEK_T_COSTDETL）质量检验

### 2.1 非空检验

| 规则编号 | 检验项 | 检验SQL | 期望结果 | 严重级别 |
|---------|--------|---------|---------|---------|
| CD-001 | 账期为空 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 账期 IS NULL OR 账期=''` | 0 | 严重 |
| CD-002 | 年度为空 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 年度 IS NULL OR 年度=''` | 0 | 严重 |
| CD-003 | 费用类别为空 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 费用类别 IS NULL OR 费用类别=''` | 0 | 严重 |
| CD-004 | 科目为空 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 科目 IS NULL OR 科目=''` | 0 | 严重 |
| CD-005 | 结算金额为空 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 结算金额 IS NULL` | 0 | 严重 |
| CD-006 | 所属业务中心为空 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 所属业务中心 IS NULL OR 所属业务中心=''` | 0 | 严重 |
| CD-007 | 事业部为空 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 事业部 IS NULL OR 事业部=''` | 0 | 严重 |
| CD-008 | 分类为空 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 分类 IS NULL OR 分类=''` | 0 | 严重 |

### 2.2 格式检验

| 规则编号 | 检验项 | 检验SQL | 期望结果 | 严重级别 |
|---------|--------|---------|---------|---------|
| CD-010 | 账期格式非YYYY-MM | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE LEN(账期)<>7 OR SUBSTRING(账期,5,1)<>'-'` | 0 | 严重 |
| CD-011 | 年度非4位数字 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE LEN(年度)<>4 OR ISNUMERIC(年度)=0` | 0 | 严重 |
| CD-012 | 结算金额为0 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 结算金额 = 0` | 0 | 警告 |
| CD-013 | XMH为空(应为public) | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE XMH IS NULL OR XMH=''` | 0 | 严重 |

### 2.3 值域检验

| 规则编号 | 检验项 | 检验SQL | 期望结果 | 严重级别 |
|---------|--------|---------|---------|---------|
| CD-020 | 费用类别不在枚举范围 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 费用类别 NOT IN ('差旅交通','员工活动','其它费用','运输费用','行政办公','招聘费用','办事处费用','物料损耗','拆机损失','辅料')` | 0 | 严重 |
| CD-021 | 分类不在枚举范围 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 分类 NOT IN ('报销','付款','阿里机票','阿里住宿','阿里打车','EBS_行政采购申请','其它_EBS付款','报废','EBS_行政出入库','付款_HRM')` | 0 | 严重 |
| CD-022 | 事业部名称未规范化 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 事业部 IN ('大客户事业部','半导体事业部','光电事业部','智能事业部','机器人事业部','计量事业部','MueTec子公司')` | 0 | 严重 |
| CD-023 | 所属业务中心含"其它" | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 所属业务中心 LIKE '%其它'` | 0 | 警告 |

### 2.4 一致性检验

| 规则编号 | 检验项 | 检验SQL | 期望结果 | 严重级别 |
|---------|--------|---------|---------|---------|
| CD-030 | 年度与账期不一致 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE LEFT(账期,4) <> 年度` | 0 | 严重 |
| CD-031 | 项目号存在但XMH为public | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 项目号 IS NOT NULL AND 项目号<>'' AND 项目号<>'public' AND XMH='public'` | 0 | 警告 |
| CD-032 | 矽行费用未打折 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL A INNER JOIN (SELECT KEYWPRD_NUM FROM TZTEK_T_KEYWORD WHERE KEYWORD_NAME='矽行系数' AND YEAR(CRE_DT)=YEAR(GETDATE())) B ON 1=1 WHERE A.事业部='矽行' AND A.分类 NOT IN ('其它_EBS付款','EBS_行政采购申请','报废_资产处置报废') AND A.结算金额 > (SELECT MAX(CAST(KEYWPRD_NUM AS FLOAT)) FROM TZTEK_T_KEYWORD WHERE KEYWORD_NAME='矽行系数') * 10000` | 0 | 警告 |

### 2.5 排除规则检验

| 规则编号 | 检验项 | 检验SQL | 期望结果 | 严重级别 |
|---------|--------|---------|---------|---------|
| CD-040 | 增值税未排除 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 科目='增值税' OR 科目 LIKE '%B01000020%'` | 0 | 严重 |
| CD-041 | 委外加工费未排除 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 订单号 IN (SELECT LCBH FROM TZTEK_T_OADT_PayWkFlow WHERE FYLB IN ('委外加工费') AND (科目 LIKE '%B0414000001%' OR 科目 LIKE '%B01000006%'))` | 0 | 严重 |
| CD-042 | 综费剔除清单未生效 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 订单号 IN (SELECT LCBH FROM TZTEK_T_COSTDETL_update WHERE 是否从综费剔除='是')` | 0 | 严重 |
| CD-043 | 资产采购未排除 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 分类='付款' AND 科目 IN ('资产采购','物料采购')` | 0 | 严重 |

---

## 三、项目综费汇总表（TZTEK_T_COST）质量检验

| 规则编号 | 检验项 | 检验SQL | 期望结果 | 严重级别 |
|---------|--------|---------|---------|---------|
| COST-001 | 结算金额汇总计算错误 | `SELECT COUNT(*) FROM TZTEK_T_COST WHERE ABS(结算金额汇总 - (结算金额 + ISNULL(业务模块分摊金额,0) + ISNULL(事业部其它分摊金额,0))) > 0.01` | 0 | 严重 |
| COST-002 | 项目号为空 | `SELECT COUNT(*) FROM TZTEK_T_COST WHERE xmh IS NULL OR xmh=''` | 0 | 严重 |
| COST-003 | Ftype不在枚举范围 | `SELECT COUNT(*) FROM TZTEK_T_COST WHERE Ftype NOT IN ('人员综费','其它综费')` | 0 | 严重 |
| COST-004 | 结算金额与COSTDETL不一致 | `SELECT COUNT(*) FROM TZTEK_T_COST A LEFT JOIN (SELECT XMH, Ftype, SUM(结算金额) AS amt FROM TZTEK_T_COSTDETL GROUP BY XMH, CASE WHEN 费用类别 IN (SELECT YM FROM TZTEK_T_KEYWORD WHERE KEYWORD_NAME='综费类别') THEN '人员综费' ELSE '其它综费' END) B ON A.xmh=B.XMH AND A.Ftype=B.Ftype WHERE ABS(A.结算金额 - ISNULL(B.amt,0)) > 0.01` | 0 | 严重 |
| COST-005 | 分摊金额为负数 | `SELECT COUNT(*) FROM TZTEK_T_COST WHERE 业务模块分摊金额<0 OR 事业部其它分摊金额<0 OR 其它级别分摊金额<0` | 0 | 警告 |
| COST-006 | 项目不在pjno清单中 | `SELECT COUNT(DISTINCT xmh) FROM TZTEK_T_COST WHERE xmh NOT IN (SELECT 项目号 FROM pjno WHERE 项目号 IS NOT NULL AND 项目号<>'')` | 0 | 警告 |

---

## 四、运营指标汇总表（TZTEK_T_OACOST）质量检验

| 规则编号 | 检验项 | 检验SQL | 期望结果 | 严重级别 |
|---------|--------|---------|---------|---------|
| OA-001 | 费用支出与COSTDETL不一致 | `SELECT A.账期, A.所属业务中心, A.事业部, A.费用支出, B.amt FROM TZTEK_T_OACOST A LEFT JOIN (SELECT 账期, 所属业务中心, 事业部, SUM(结算金额) AS amt FROM TZTEK_T_COSTDETL GROUP BY 账期, 所属业务中心, 事业部) B ON A.账期=B.账期 AND A.所属业务中心=B.所属业务中心 AND A.事业部=B.事业部 WHERE ABS(A.费用支出 - ISNULL(B.amt,0)) > 0.01` | 0行 | 严重 |
| OA-002 | 账期格式异常 | `SELECT COUNT(*) FROM TZTEK_T_OACOST WHERE LEN(账期)<>7` | 0 | 严重 |
| OA-003 | 费用支出为0 | `SELECT COUNT(*) FROM TZTEK_T_OACOST WHERE 费用支出=0 OR 费用支出 IS NULL` | 0 | 警告 |

---

## 五、特别成本表（TZTEK_T_COSTDETL_Specialcosts）质量检验

| 规则编号 | 检验项 | 检验SQL | 期望结果 | 严重级别 |
|---------|--------|---------|---------|---------|
| SP-001 | 是否特别成本为空 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL_Specialcosts WHERE 是否特别成本 IS NULL OR 是否特别成本=''` | 0 | 严重 |
| SP-002 | 是否特别成本不在枚举范围 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL_Specialcosts WHERE 是否特别成本 NOT IN ('是','否','数据修复')` | 0 | 严重 |
| SP-003 | 存在"数据修复"记录 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL_Specialcosts WHERE 是否特别成本='数据修复'` | 0 | 警告 |
| SP-004 | 服务费与业务支持合同重复 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL_Specialcosts WHERE 科目='服务费' AND 分类='付款' AND 订单号 IN (SELECT LCBH FROM TZTEK_T_OADT_PayWkFlow WHERE gllc IN (SELECT CAST(REQUESTID AS NVARCHAR) FROM TZTEK_T_YYZB_OA_HTSPSQLC))` | 0 | 严重 |
| SP-005 | NRE费用分类异常 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL_Specialcosts WHERE 科目='NRE费用' AND 分类<>'付款' AND 年度='2026'` | 0 | 严重 |

---

## 六、现金流表（TZTEK_T_COSTDETL_CashFlow）质量检验

| 规则编号 | 检验项 | 检验SQL | 期望结果 | 严重级别 |
|---------|--------|---------|---------|---------|
| CF-001 | 结算金额非数字 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL_CashFlow WHERE ISNUMERIC(结算金额)=0` | 0 | 严重 |
| CF-002 | 现金流总额应大于综费总额 | `SELECT CASE WHEN (SELECT SUM(CAST(结算金额 AS FLOAT)) FROM TZTEK_T_COSTDETL_CashFlow WHERE LEFT(账期,4)='2026') < (SELECT SUM(结算金额) FROM TZTEK_T_COSTDETL WHERE LEFT(账期,4)='2026') THEN 1 ELSE 0 END` | 0 | 严重 |

---

## 七、跨表一致性检验

| 规则编号 | 检验项 | 检验SQL | 期望结果 | 严重级别 |
|---------|--------|---------|---------|---------|
| CROSS-001 | COSTDETL项目号在项目主数据中存在 | `SELECT COUNT(DISTINCT XMH) FROM TZTEK_T_COSTDETL WHERE XMH<>'public' AND XMH NOT IN (SELECT XMH FROM TZTEK_V_LXXMHXX_ALL)` | 0 | 警告 |
| CROSS-002 | COSTDETL入职代码在人员映射表中存在 | `SELECT COUNT(DISTINCT 预定人入职代码) FROM TZTEK_T_COSTDETL WHERE 预定人入职代码 IS NOT NULL AND 预定人入职代码<>'' AND 预定人入职代码 NOT IN (SELECT RZDM FROM TZTEK_T_BUSMODULE)` | 0 | 警告 |
| CROSS-003 | COSTDETL费用类别在映射表中存在 | `SELECT COUNT(DISTINCT 费用类别) FROM TZTEK_T_COSTDETL WHERE 费用类别 NOT IN (SELECT CostType FROM TZTEK_T_CostType_Account)` | 0 | 警告 |
| CROSS-004 | 矽行系数配置存在 | `SELECT COUNT(*) FROM TZTEK_T_KEYWORD WHERE KEYWORD_NAME='矽行系数' AND YEAR(CRE_DT)=YEAR(GETDATE())` | >0 | 严重 |
| CROSS-005 | 综费类别配置存在 | `SELECT COUNT(*) FROM TZTEK_T_KEYWORD WHERE KEYWORD_NAME='综费类别'` | >=5 | 严重 |
| CROSS-006 | pjno表有数据 | `SELECT COUNT(*) FROM pjno WHERE 项目号 IS NOT NULL AND 项目号<>''` | >0 | 严重 |
| CROSS-007 | StartDate参数存在 | `SELECT COUNT(*) FROM TZTEK_Pro_SalesManagement_StartDate WHERE startdate IS NOT NULL` | >0 | 严重 |

---

## 八、业务逻辑检验

| 规则编号 | 检验项 | 检验SQL | 期望结果 | 严重级别 |
|---------|--------|---------|---------|---------|
| BIZ-001 | 投标保证金净额应为0或接近0(长期) | `SELECT SUM(结算金额) FROM TZTEK_T_COSTDETL WHERE 科目='竞标/投标保证金' AND LEFT(账期,4)<'2026'` | 接近0 | 警告 |
| BIZ-002 | 水电煤分摊总金额与原始金额一致 | `SELECT ABS((SELECT SUM(结算金额) FROM TZTEK_T_COSTDETL WHERE 科目='水电煤' AND 分类='付款') - (SELECT SUM(结算金额) FROM TZTEK_T_COSTDETL WHERE 科目='水电煤'))` | <1 | 严重 |
| BIZ-003 | MueTec费用汇率应为7.5 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 所属业务中心='MueTec子公司' AND 分类='付款' AND 结算金额 > 0 AND ABS(结算金额 / (SELECT TOP 1 金额 FROM link_u8if.reportnew.dbo.TZTEK_T_MUETEC_RECEIPTS_DISBURSE WHERE ATTRIBUTE1=账期) - 7.5) > 0.1` | 0 | 警告 |
| BIZ-004 | 当月数据已生成 | `SELECT COUNT(*) FROM TZTEK_T_COSTDETL WHERE 账期 = FORMAT(GETDATE(),'yyyy-MM')` | >0 | 警告 |
| BIZ-005 | 特别成本中服务费占比异常(>50%) | `SELECT CASE WHEN (SELECT SUM(结算金额) FROM TZTEK_T_COSTDETL_Specialcosts WHERE 科目='服务费') / NULLIF((SELECT SUM(结算金额) FROM TZTEK_T_COSTDETL_Specialcosts),0) > 0.5 THEN 1 ELSE 0 END` | 0 | 警告 |
